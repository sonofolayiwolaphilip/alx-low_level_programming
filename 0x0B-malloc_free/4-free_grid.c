#include <stdio.h>
#include <stdlib.h>
/**
 * alloc_grid - grid
 * @width: parameter
 * @height: parameter
 * Return: NULL on failure
 */
void free_grid(int **grid, int height)
{
	if (width <= 0 || height <= 0)
	{
		return (NULL);
	}
	int **grid = (int **)malloc(height * sizeof(int *));
	if (grid == NULL)
	{
}
